# ChefMentor X - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection string
# Format: postgresql+asyncpg://user:password@host:port/database
# Local: postgresql+asyncpg://postgres:password@localhost:5432/chefmentor_dev
# Railway: Get from Railway dashboard after provisioning PostgreSQL
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/chefmentor_dev

# Redis connection string
# Local: redis://localhost:6379/0
# Upstash: Get from Upstash dashboard (format: rediss://...)
REDIS_URL=redis://localhost:6379/0

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# JWT Secret - Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# IMPORTANT: Use a strong, random secret in production!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15

# Google OAuth 2.0 Credentials
# Get from: https://console.cloud.google.com/
# 1. Create new project or select existing
# 2. APIs & Services → Credentials → Create OAuth 2.0 Client ID
# 3. Configure consent screen
# 4. Add authorized redirect URIs:
#    - http://localhost:8000/auth/google/callback
#    - exp://localhost:19000
GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# =============================================================================
# FILE STORAGE - CLOUDINARY
# =============================================================================
# Get from: https://cloudinary.com/console
# Sign up for free account (up to 25 GB storage, 25 GB bandwidth/month)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# =============================================================================
# AI/ML SERVICES
# =============================================================================
# Google Gemini API Key (Required)
# Get from: https://aistudio.google.com/app/apikey
# Free tier: 60 requests per minute
# Used for: AI cooking guidance, recipe suggestions, failure analysis
GEMINI_API_KEY=your-gemini-api-key-here

# Groq API Key (Required)
# Get from: https://console.groq.com/keys
# Free tier: Available with rate limits
# Used for: Whisper speech-to-text processing
GROQ_API_KEY=your-groq-api-key-here

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================
# Recipe Database APIs (Optional - can use seed data instead)
RECIPE_DB_API_KEY=
FLAVOR_DB_API_KEY=

# PostHog Analytics (Optional)
# Get from: https://posthog.com/
POSTHOG_API_KEY=
POSTHOG_HOST=

# Sentry Error Tracking (Optional)
# Get from: https://sentry.io/
SENTRY_DSN=

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Environment: development, staging, production
ENVIRONMENT=development

# Debug mode (true/false)
DEBUG=true

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Development: Use "*" for permissive mode (automatically handled by code)
# Production: MUST be comma-separated list of exact origins (NO wildcards!)
# 
# Examples:
# - Development: CORS_ORIGINS=*
# - Production: CORS_ORIGINS=https://api.example.com,https://app.example.com
# - Railway: CORS_ORIGINS=https://chefmentor-api.railway.app
CORS_ORIGINS=*

# Allowed host patterns (development only, ignored in production)
ALLOWED_HOSTS=localhost,127.0.0.1,192.168.*.*,10.0.*.*,172.16.*.*

# =============================================================================
# NOTES & INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env in the same directory
# 2. Fill in your actual API keys and secrets
# 3. Never commit .env to version control (already in .gitignore)
# 4. For production, generate new secrets and use secure values
# 5. Rotate API keys periodically for security
#
# Quick Setup Commands:
# - Generate JWT Secret: python -c "import secrets; print(secrets.token_hex(32))"
# - Test connection: python -c "from dotenv import load_dotenv; load_dotenv(); import os; print(os.getenv('DATABASE_URL'))"
#
# Database Setup:
# - Local PostgreSQL: Create database with: CREATE DATABASE chefmentor_dev;
# - Cloud: Use Railway (railway.app) or Supabase (supabase.com) for free tier
# - Redis: Use Upstash (upstash.com) for free serverless Redis
